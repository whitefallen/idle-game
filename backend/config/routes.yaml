# yaml-language-server: $schema=../vendor/symfony/routing/Loader/schema/routing.schema.json

# Controllers live in each feature's Http layer rather than a global
# src/Controller directory, so routing scans the feature tree.
# See docs/architecture.md section 3.
controllers:
    resource:
        path: ../src/Feature/
        namespace: App\Feature
    type: attribute
