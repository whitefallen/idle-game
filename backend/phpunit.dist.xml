<?xml version="1.0" encoding="UTF-8"?>

<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         colors="true"
         failOnDeprecation="true"
         failOnNotice="true"
         failOnWarning="true"
         failOnRisky="true"
         beStrictAboutOutputDuringTests="true"
         bootstrap="tests/bootstrap.php"
         cacheDirectory=".phpunit.cache"
>
    <php>
        <ini name="display_errors" value="1" />
        <ini name="error_reporting" value="-1" />
        <!--
            Must be <env>, not <server>: KernelTestCase resolves the environment
            from $_ENV before $_SERVER, and the container exports APP_ENV=dev.
            A <server> entry would leave the kernel booting in dev, silently
            losing the test-only services.
        -->
        <env name="APP_ENV" value="test" force="true" />
        <!--
            Debug off, which is a performance decision rather than a stylistic
            one. With debug on, Symfony re-checks every cached config resource
            on every kernel boot, and KernelTestCase boots per test: measured on
            this project a debug boot costs 966ms against 22ms without it.

            The trade-off is that the compiled container is no longer rebuilt
            when its sources change, so after editing service configuration you
            must clear the test cache by hand (bin/console cache:clear, with the
            env option set to test) or the suite will run against a stale
            container. Content YAML is unaffected — ContentSource reads it at
            runtime rather than compiling it in — and CI always starts from a
            fresh container, so this only bites locally.
        -->
        <env name="APP_DEBUG" value="0" force="true" />
        <env name="KERNEL_CLASS" value="App\Kernel" />
        <server name="SHELL_VERBOSITY" value="-1" />
    </php>

    <testsuites>
        <!--
            Unit: pure domain logic. No kernel, no database, no filesystem.
            This suite must stay fast enough to run on every save.
        -->
        <testsuite name="unit">
            <directory>tests/Unit</directory>
        </testsuite>

        <!-- Integration: application layer against a real kernel and database. -->
        <testsuite name="integration">
            <directory>tests/Integration</directory>
        </testsuite>

        <!-- Functional: HTTP contract and error shapes. -->
        <testsuite name="functional">
            <directory>tests/Functional</directory>
        </testsuite>
    </testsuites>

    <source ignoreSuppressionOfDeprecations="true"
            ignoreIndirectDeprecations="true"
            restrictNotices="true"
            restrictWarnings="true"
    >
        <include>
            <directory>src</directory>
        </include>

        <deprecationTrigger>
            <method>Doctrine\Deprecations\Deprecation::trigger</method>
            <method>Doctrine\Deprecations\Deprecation::delegateTriggerToBackend</method>
            <function>trigger_deprecation</function>
        </deprecationTrigger>
    </source>

    <extensions>
        <bootstrap class="Symfony\Bridge\PhpUnit\SymfonyExtension">
            <parameter name="clock-mock-namespaces" value="App" />
            <parameter name="dns-mock-namespaces" value="App" />
        </bootstrap>
    </extensions>
</phpunit>
